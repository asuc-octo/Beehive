***REMOVED***!/usr/bin/env ruby
require 'pathname'

***REMOVED*** path to your application root.
APP_ROOT = Pathname.new File.expand_path('../../',  __FILE__)

Dir.chdir APP_ROOT do
  ***REMOVED*** This script is a starting point to setup your application.
  ***REMOVED*** Add necessary setup steps to this file:

  puts "== Installing dependencies =="
  system "gem install bundler --conservative"
  system "bundle check || bundle install"

  ***REMOVED*** puts "\n== Copying sample files =="
  ***REMOVED*** unless File.exist?("config/database.yml")
  ***REMOVED***   system "cp config/database.yml.sample config/database.yml"
  ***REMOVED*** end

  puts "\n== Preparing database =="
  system "bin/rake db:setup"

  puts "\n== Removing old logs and tempfiles =="
  system "rm -f log/*"
  system "rm -rf tmp/cache"

  puts "\n== Restarting application server =="
  system "touch tmp/restart.txt"
end
