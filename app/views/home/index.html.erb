<% content_for :title, "Welcome" %>
<% if @current_user.present? %>
  <%= react_component("Home", {job_count:Job.find_jobs.count,
                             present: @current_user.present?,
                             undergrad: @current_user.undergrad?,
                             jobs_path: jobs_path,
                             dashboard_path: dashboard_path,
                             login_path: login_path,
                             team_path: team_path}) %>
<% else %>
  <%= react_component("Home", {job_count:Job.find_jobs.count,
                             present: @current_user.present?,
                             jobs_path: jobs_path,
                             dashboard_path: dashboard_path,
                             login_path: login_path,
                             team_path: team_path}) %>
<% end %>
<!--<% if @current_user.present? && @current_user.undergrad? %>
      <%= link_to "EXPLORE", jobs_path, class: "btn btn-cal btn-landing" %>
    <% elsif @current_user.present? %>
      <%= link_to "EXPLORE", dashboard_path, class: "btn btn-cal btn-landing" %>
    <% else %>
      <%= link_to "EXPLORE", login_path, class: "btn btn-cal btn-landing" %>
    <% end %>
    <%= link_to "ABOUT US", team_path, class: "btn btn-cal btn-landing" %>-->